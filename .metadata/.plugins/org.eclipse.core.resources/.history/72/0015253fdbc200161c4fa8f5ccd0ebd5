package DungeonsAndZombies;

public enum AttackTypes {
	WEAPON, MAGIC, INVALID;
	
	public AttackTypes parseType(String type) {
		if (type == "weapon") {
			return WEAPON;
		} else if (type == "magic") {
			return MAGIC;
		} else {
			return INVALID;
		}
	}
}
